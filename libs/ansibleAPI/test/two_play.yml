---

- name: Test the plabybook API.
  hosts: all
  remote_user: root
  gather_facts: yes
  tasks:
   - name: uptime
     shell: uptime


- name: Second test
  hosts: all
  remote_user: root
  gather_facts: yes
  tasks:
   - name: hostname
     shell: hostname 

